<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - Order #<%= orderNumber %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #2c3e50;
            max-width: 700px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ecf0f1;
        }
        .container {
            background-color: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            border-bottom: 3px solid #3498db;
            padding-bottom: 25px;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #3498db;
            margin: 0;
            font-size: 32px;
            font-weight: 700;
        }
        .invoice-title {
            color: #7f8c8d;
            font-size: 18px;
            margin-top: 5px;
        }
        .invoice-details {
            display: flex;
            justify-content: space-between;
            margin: 30px 0;
            flex-wrap: wrap;
        }
        .invoice-info, .customer-info {
            flex: 1;
            min-width: 250px;
            margin: 10px;
        }
        .invoice-info h3, .customer-info h3 {
            color: #2c3e50;
            border-bottom: 2px solid #3498db;
            padding-bottom: 8px;
            margin-bottom: 15px;
        }
        .info-row {
            margin: 8px 0;
            display: flex;
            justify-content: space-between;
        }
        .info-label {
            font-weight: 600;
            color: #7f8c8d;
        }
        .info-value {
            font-weight: 500;
            color: #2c3e50;
        }
        .restaurant-info {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 20px;
            border-radius: 12px;
            margin: 25px 0;
            text-align: center;
        }
        .restaurant-info h3 {
            margin: 0 0 10px 0;
            font-size: 24px;
        }
        .restaurant-info p {
            margin: 5px 0;
            opacity: 0.9;
        }
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin: 25px 0;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .items-table th {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 15px 12px;
            text-align: left;
            font-weight: 600;
        }
        .items-table td {
            padding: 15px 12px;
            text-align: left;
            border-bottom: 1px solid #ecf0f1;
        }
        .items-table tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        .items-table tr:hover {
            background-color: #e3f2fd;
        }
        .special-instructions {
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 10px 15px;
            margin: 5px 0;
            border-radius: 4px;
        }
        .price-breakdown {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin: 25px 0;
        }
        .price-row {
            display: flex;
            justify-content: space-between;
            margin: 8px 0;
            padding: 5px 0;
        }
        .price-row.total {
            border-top: 2px solid #3498db;
            padding-top: 15px;
            margin-top: 15px;
            font-size: 20px;
            font-weight: 700;
            color: #2c3e50;
        }
        .status-badge {
            display: inline-block;
            padding: 8px 20px;
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .footer {
            text-align: center;
            margin-top: 35px;
            padding-top: 25px;
            border-top: 2px solid #ecf0f1;
            color: #7f8c8d;
        }
        .footer h4 {
            color: #3498db;
            margin-bottom: 10px;
        }
        .qr-section {
            text-align: center;
            margin: 20px 0;
            padding: 20px;
            background-color: #f8f9fa;
            border-radius: 8px;
        }
        .qr-code {
            width: 120px;
            height: 120px;
            margin: 0 auto;
            background-color: #ddd;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🧾 INVOICE</h1>
            <p class="invoice-title">Order Confirmation & Receipt</p>
        </div>

        <div class="invoice-details">
            <div class="invoice-info">
                <h3>📋 Invoice Details</h3>
                <div class="info-row">
                    <span class="info-label">Invoice #:</span>
                    <span class="info-value"><%= orderNumber %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Date:</span>
                    <span class="info-value"><%= orderDate %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Time:</span>
                    <span class="info-value"><%= orderTime %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Status:</span>
                    <span class="info-value"><span class="status-badge">Confirmed</span></span>
                </div>
                <% if (tableNumber) { %>
                <div class="info-row">
                    <span class="info-label">Table:</span>
                    <span class="info-value">#<%= tableNumber %></span>
                </div>
                <% } %>
            </div>

            <div class="customer-info">
                <h3>👤 Customer Details</h3>
                <div class="info-row">
                    <span class="info-label">Name:</span>
                    <span class="info-value"><%= customerName %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Email:</span>
                    <span class="info-value"><%= customerEmail %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Phone:</span>
                    <span class="info-value"><%= customerPhone %></span>
                </div>
            </div>
        </div>

        <div class="restaurant-info">
            <h3>🏪 <%= restaurantName %></h3>
            <p>Your order has been received and is being prepared!</p>
            <p>We'll notify you when it's ready for pickup/delivery</p>
        </div>

        <h3>📋 Order Items</h3>
        <table class="items-table">
            <thead>
                <tr>
                    <th>Item</th>
                    <th>Qty</th>
                    <th>Unit Price</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach(function(item) { %>
                    <tr>
                        <td>
                            <strong><%= item.menuItem.name %></strong>
                        </td>
                        <td><%= item.quantity %></td>
                        <td>₹<%= item.price.toFixed(2) %></td>
                        <td><strong>₹<%= (item.price * item.quantity).toFixed(2) %></strong></td>
                    </tr>
                    <% if (item.specialInstructions) { %>
                        <tr>
                            <td colspan="4">
                                <div class="special-instructions">
                                    📝 <strong>Special Instructions:</strong> <%= item.specialInstructions %>
                                </div>
                            </td>
                        </tr>
                    <% } %>
                <% }); %>
            </tbody>
        </table>

        <div class="price-breakdown">
            <h3>💰 Price Breakdown</h3>
            <div class="price-row">
                <span>Subtotal:</span>
                <span>₹<%= (totalAmount / 1.18).toFixed(2) %></span>
            </div>
            <div class="price-row">
                <span>GST (18%):</span>
                <span>₹<%= (totalAmount - (totalAmount / 1.18)).toFixed(2) %></span>
            </div>
            <div class="price-row total">
                <span>Total Amount:</span>
                <span>₹<%= totalAmount.toFixed(2) %></span>
            </div>
        </div>

        <% if (notes) { %>
        <div class="special-instructions">
            <strong>📝 Additional Notes:</strong> <%= notes %>
        </div>
        <% } %>

        <div class="qr-section">
            <h4>📱 Track Your Order</h4>
            <p>Use this order number to track your order status</p>
            <div class="qr-code">
                <%= orderNumber %>
            </div>
        </div>

        <div class="footer">
            <h4>Thank you for choosing <%= restaurantName %>!</h4>
            <p>We appreciate your business and hope you enjoy your meal.</p>
            <p>For any questions, please contact us or visit our restaurant.</p>
            <p><small>This is an automatically generated invoice. No signature required.</small></p>
        </div>
    </div>
</body>
</html>
